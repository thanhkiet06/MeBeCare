@model IEnumerable<MeBeCare.Models.MedicalService>
@{
    ViewBag.Title = "Tìm kiếm Dịch vụ Y tế";
}

<h2 class="mb-4 text-pink">🔎 Tìm kiếm Dịch vụ Y tế</h2>

<form method="get" asp-action="Index" class="row mb-4">
    <div class="col-md-4">
        <input type="text" name="searchString" value="@ViewBag.SearchString" class="form-control border-pink" placeholder="🔍 Nhập tên hoặc loại dịch vụ...">
    </div>
    <div class="col-md-3">
        <select name="typeFilter" class="form-select border-pink">
            <option value="All" selected="@(ViewBag.TypeFilter == "All")">Tất cả</option>
            <option value="Hospital" selected="@(ViewBag.TypeFilter == "Hospital")">Bệnh viện</option>
            <option value="Clinic" selected="@(ViewBag.TypeFilter == "Clinic")">Phòng khám</option>
            <option value="Pharmacy" selected="@(ViewBag.TypeFilter == "Pharmacy")">Nhà thuốc</option>
            <option value="Other" selected="@(ViewBag.TypeFilter == "Other")">Khác</option>
        </select>
    </div>
    <div class="col-md-2">
        <button type="submit" class="btn btn-pink w-100">Tìm kiếm</button>
    </div>
</form>

@if (!Model.Any())
{
    <div class="alert alert-warning">❗ Không tìm thấy dịch vụ phù hợp.</div>
}
else
{
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm border-pink">
                    <div class="card-body">
                        <h5 class="card-title text-pink">@item.Name</h5>
                        <p class="card-text"><strong>🏷️ Loại:</strong> @item.Type</p>
                        <p class="card-text"><strong>📍 Địa chỉ:</strong> @item.Address</p>
                        <p class="card-text"><strong>📞 Điện thoại:</strong> @item.Phone</p>
                        @if (!string.IsNullOrEmpty(item.Description))
                        {
                            <p class="card-text"><strong>📝 Mô tả:</strong> @item.Description</p>
                        }
                        @if (item.Rating != null)
                        {
                            <p class="card-text">⭐ <strong>@item.Rating</strong> / 5.0</p>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
}

<div class="alert alert-light border border-pink mt-4">
    <h5 class="text-pink">📊 Tổng quan:</h5>
    <ul>
        <li><strong>Tổng số dịch vụ:</strong> @Model.Count()</li>
        <li><strong>Loại dịch vụ:</strong> @ViewBag.TypeFilter</li>
    </ul>
</div>

<div class="text-muted small mt-2">
    💡 Bạn có thể tìm kiếm theo tên, loại hoặc vị trí dịch vụ.<br />
    📌 Hệ thống liên tục cập nhật dịch vụ mới từ các đối tác uy tín.<br />
    ❓ Nếu cần hỗ trợ, vui lòng liên hệ đội ngũ hỗ trợ kỹ thuật.
</div>

<div class="mt-4">
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">
        🏠 Quay về Trang Chủ
    </a>
</div>
